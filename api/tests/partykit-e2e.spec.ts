import { describe, it } from 'vitest';

// Placeholder: PartyServer end-to-end coverage requires Workers runtime.
describe.skip('Partykit game room end-to-end flow (Workers runtime)', () => {
	it('runs in Workers environment', () => {
		// Verified via deployed Worker/PartyServer integration.
	});
});
